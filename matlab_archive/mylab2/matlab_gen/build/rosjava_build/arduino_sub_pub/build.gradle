/*
 * Copyright 2015 The MathWorks, Inc.
 */

try {
    project.catkin.tree.generateMessageArtifact(project, 'arduino_sub_pub')
} catch (NullPointerException e) {
    println("Couldn't find arduino_sub_pub on the ROS_PACKAGE_PATH")
}

task bugfixtask << {
    mkdir sourceSets.main.output.classesDir
}
jar.dependsOn(bugfixtask)
